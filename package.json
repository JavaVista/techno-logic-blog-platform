{
  "name": "techno-logic-blog-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "cms"
  ],
  "scripts": {
    "prepare": "husky",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces",
    "build": "npm run build --workspaces",
    "start": "npm run start --workspaces",
    "test": "npm run test --workspaces"
  },
  "lint-staged": {
    "*.{ts,js,tsx,jsx,html,scss,css,md,json}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.5",
    "prettier": "^3.6.2"
  }
}
